# Thread 'engRAM - superior agentic memory, encrypted at rest' in forum #community-projects-showcase
# thread_id=1528807731065262261 forum_id=1316137596535177246
# Started 2026-07-20T19:41:21.812531+00:00

[2026-07-20T16:56:06.960000+00:00] maxfreedompollard (id=1528807731065262261)
    engRAM is agentic vector memory that stores greater context more efficiently than other memory backends: decisions bound to their questions, importance-weighted hybrid recall, and AEAD on every byte at rest - including the embedding vectors. 100% offline, 100% portable, 100% encrypted. 
    
    Cross platform - one vector memory file can be used by Hermes, Claude, OpenClaw, and other agents all at once. 
    
    Memory behavior
    • Short answers are not discarded - "OK" is stored with the question it answered, so you don't need to repeat yourself or remember how to do the same task twice. 
    • Deterministic importance ranks recall (consent/decisions and personal/machine context above noise)
    • Hybrid search (vector + keyword), exact SIMD top-k (recall 1.0 by construction)
    • Pinned embedding model (SHA-256 enforced) so cosine space does not silently rot
    • No LLM inside the memory layer - capture/rank/encrypt are deterministic and reproducible
    
    Security / offline
    • Vectors encrypted at rest (most memory stacks leave them plaintext)
    • Crypto-shred forget: destroy the per-record key, content is gone
    • Fully offline: bundled ONNX embedder, runtime aborts on any network attempt
    • Lock/unlock on demand; default unlock stays open until restart or you lock
    • Hash-chained audit, per-caller namespaces, portable single-file vault
    
    Agent wiring
    • Hermes native provider - hermes memory setup, "no setup needed" (no API key/cloud/daemon)
    • Claude + OpenClaw via MCP; CLI for anything else
    • One vault shared across agents and machines (lock → move file → unlock)
    
    
    https://github.com/MaxFreedomPollard/engRAM
    [embed] GitHub - MaxFreedomPollard/engRAM: engRAM - superior agentic memory... https://github.com/MaxFreedomPollard/engRAM

[2026-07-21T02:28:38.463000+00:00] maxfreedompollard (id=1528951811711701122)
    Added live dashboard with how many memories of what kind, growth over time, the relation graph, top tags, per-agent counts, and live hybrid search. Served entirely from RAM over 127.0.0.1 behind a random URL token - read only, zero outbound requests, zero configuration, stops with Ctrl-C.

[2026-07-21T02:33:05.205000+00:00] maxfreedompollard (id=1528952930508472511)
    Another update: Added Two Factor Unlock, so you can use 2FA for your vector memory. I see vector memory as master memory - it contains all data from your computer, your storage, the internet, your mind, it's by far the most important memory store for someone using agentic memory. With 2FA you can trust it a bit more.

[2026-07-21T02:35:50.050000+00:00] maxfreedompollard (id=1528953621918646422)
    (This little update is not a feature required by 99% of users, just for techies who are curious, it is not visible or annoying to normal users)
    
    Test performance yourself. "engram setup download-longmemeval" can now fetch the public LongMemEval dataset;
    engram bench --longmemeval scores the exact production retrieval
    pipeline against it, with a checkpointed on-disk vector cache so re-runs
    take seconds. Pure tooling - zero runtime impact.

[2026-07-21T02:38:58.884000+00:00] maxfreedompollard (id=1528954413945979053)
    https://klipy.com/gifs/cleavage-bosom
    [embed] Cleavage Bosom https://klipy.com/gifs/cleavage-bosom

[2026-07-21T02:39:37.310000+00:00] maxfreedompollard (id=1528954575116177438)
    Update: Memory graph.
    
    Explicit relations between things the vault knows: memory_link memory_relations  memory_unlink over MCP, engram link / relations / unlink from the CLI. Subject-predicate-object with an optional source memory and validity window; entity matching is case-insensitive, re-links are idempotent, and as_of answers "what was true then". Deterministic storage with the same encryption, ACL, audit, and fsync'd crash-journal guarantees as every record - the judgment of what to link stays with the host model.

