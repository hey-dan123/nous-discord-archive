# Thread 'Hermes - Toolaria - Tool-Call Truncator' in forum #plugins-skills-and-skins
# thread_id=1514704726682243142 forum_id=1485392832154832906
# Started 2026-06-11T20:23:25.886680+00:00

[2026-06-11T18:55:48.694000+00:00] sahil_ss9 (id=1514704726682243142)
    Toolaria 🛟 — stop oversized tool results flooding your context
    
      Built a little Hermes plugin. MCP and web tool calls (web_extract, browser snapshots, big JSON) dodge the normal truncation and dump 50-200k chars straight into context. Toolaria catches them at the
      transform_tool_result hook, spills the full thing to a SHA256 blob store, and hands the model a compact excerpt plus a fetch handle.
    
      The model then pulls only what it needs:
      rescuer_fetch(id, mode="grep"|"range"|"stat"|"full")
    
      It's the spill-to-disk pattern Claude Code uses, packaged as one zero-config plugin. On by default, no engine swap, composes with whatever context setup you run. Does one thing.
    
      - SHA256-addressed, dedupes identical results
      - grep runs under a real timeout (no ReDoS foot-guns)
      - swept blobs leave a breadcrumb telling the model to re-run the source tool, not a dead handle
    
      Clone, enable, hermes plugin reload. That's it.
    
      👉 github.com/Sahil-SS9/Toolaria
    
      Feedback welcome, especially if you run it next to a heavier context engine
    [attachment] Toolaria.png https://cdn.discordapp.com/attachments/1514704726682243142/1514704727844192408/Toolaria.png?ex=6a2c5634&is=6a2b04b4&hm=98de8f0cecfe89b3fcdc49f08aadf855ed0cfd0ec89e9185dac1108de61db7bc&

[2026-06-11T18:56:22.042000+00:00] sahil_ss9 (id=1514704866553892876)
    https://github.com/Sahil-SS9/Toolaria
    [embed] GitHub - Sahil-SS9/Toolaria: Rescue oversized tool results before t... https://github.com/Sahil-SS9/Toolaria

[2026-06-12T03:24:44.832000+00:00] isyutaru612 (id=1514832804528259224)
    All setup ty
    [attachment] image.png https://cdn.discordapp.com/attachments/1514704726682243142/1514832804058239126/image.png?ex=6a2ccd7c&is=6a2b7bfc&hm=ccf1c65e4291c38e6c47482bf0c302051aa12f932c0e23918bc6119a705199ed&

[2026-06-12T09:05:31.592000+00:00] sahil_ss9 (id=1514918564455518238)
    Toolaria v2 is live
    
    Quick recap: big tool results (web pages, API dumps, MCP output) used to flood the model's context and crowd everything else out. Toolaria catches them, stashes the full thing on disk, and hands the model a tiny handle instead.
    
    v2 turns that handle into a proper workspace:
    
    Outline — get a structural map of a result without reading it. JSON schemas with number stats, page headings, log error clusters. The model navigates by structure, not guesswork.
    
    Search — ask a rescued result a question in plain English, get the relevant bits back with line numbers. Works dep-free out of the box, smarter with embeddings installed.
    
    Pass-by-reference — the big one. Hand a whole result to another tool without the model ever reading it. Write tla:<id> and Toolaria swaps in the full content right before the tool runs.
    
    The numbers, on a real 2,000-row result:
    - 171,874 chars became a 1,733-char handle. 99x smaller in context.
    - Passed it to a second tool by emitting 79 characters. The tool still got all 171,874.
    
    Hardened too: session-scoped so one session can't pull another's data, exec/exfil sinks blocked by default, grep that can't be hung by a nasty regex.
    
    Zero config, on by default, composes with whatever context engine you run.
    
    Repo and a Proof section showing it in action: https://github.com/Sahil-SS9/Toolaria
    [embed] GitHub - Sahil-SS9/Toolaria: Rescue oversized tool results before t... https://github.com/Sahil-SS9/Toolaria

[2026-06-12T18:50:45.132000+00:00] eucdee (id=1515065841316528330)
    I told hermes to setup and use it.. 
    
    **Toolaria is now fully set up and working!** Here's a summary:
    
    ## ✅ Setup Complete
    
    **Installation steps performed:**
    1. Cloned `https://github.com/Sahil-SS9/Toolaria.git` to `~/.hermes/plugins/toolaria`
    2. Installed dependencies (`pip install -r requirements.txt`)
    3. Created `plugin.yaml` manifest (required for plugin discovery)
    4. Added `toolaria` to `plugins.enabled` in `~/.hermes/config.yaml`
    5. Restarted the gateway via `systemctl restart hermes-gateway`
    
    Nice 🙂

[2026-06-13T13:55:41.403000+00:00] sahil_ss9 (id=1515353974360768523)
    **Toolaria v3 is live** — `Sahil-SS9/Toolaria@0dcb310`
    
    **What's new (5 features):**
    
    🔹 **Hot-blob pinning** — Frequently-fetched blobs survive TTL sweeps. A blob you keep querying won't vanish at 72h. Uses recency-weighted counting (not dumb threshold) so old fetches naturally decay. Config: `hot_fetch_threshold: 3.0`, `hot_ttl_hours: 168`
    
    🔹 **Anchor-pattern excerpts** — Error/decision/action/value lines promoted into excerpts even outside head/tail windows. "The team decided to deploy" or "Total: 42 items" now surface automatically. Config: `anchor_patterns: {error, decision, action, value}`
    
    🔹 **Provenance enrichment** — Rescue handles now show `source=` (URL from original tool args) and `at=` (UTC timestamp). Model can assess freshness without fetching.
    
    🔹 **Semantic role tagging** — Handles include `role=episodic|semantic|procedural`. web_search → episodic, rescuer_fetch outline → semantic. Helps models understand what kind of data a blob holds.
    
    🔹 **Chain mode** — `mode="chain"` does grep→context in a single call. No more separate grep + range calls just to see what's around an error. Merges overlapping windows. Usage: `rescuer_fetch(id="abc", mode="chain", pattern="ERROR", count=3)`
    
    **How to update:** `git pull origin main` if you track the repo, or copy the plugin files. Restart your gateway for plugin reload.
    
    **Tests:** 94/94. Backward compatible — all 5 features disabled by default until you set the config keys.

[2026-06-24T19:31:21.657000+00:00] sahil_ss9 (id=1519424715230548115)
    Toolaria v3.1.0 is out 🧰
    
    Quick one for anyone using it: Toolaria rescues oversized tool results to disk
    and hands the model a small excerpt plus a fetch handle, so big web/MCP outputs
    stop flooding your context window.
    
    This release fixes a sharp edge. The fetch tool (rescuer_fetch) wasn't
    reachable in toolset-restricted sessions like cron jobs and scoped profiles, so
    handles raised there couldn't be redeemed. It's now an ambient tool, always
    available wherever a rescue can happen. And if a host can't guarantee that,
    Toolaria says so loudly at load instead of failing quietly.
    
    A handle you can't fetch is worse than no rescue. That's the whole fix.
    
    Grab it: https://github.com/Sahil-SS9/Toolaria
    
    It's deliberately light: the headline win, the honest framing, no internals. Want me to also post it to a channel, or is generating it enough? And separately, say the word and I'll run the deploy checklist (merge KenseiAgent branch, sync the live plugin copy, restart gateways) to take the full fix live on your box.
    [embed] GitHub - Sahil-SS9/Toolaria: Rescue oversized tool results before t... https://github.com/Sahil-SS9/Toolaria

[2026-07-03T23:59:50.433000+00:00] alto69 (id=1522753771124293823)
    sounds nice. whats the risk something gets lost

